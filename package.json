{
  "name": "hotel-booking",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/*-web\" dev",
    "build": "pnpm -r --filter \"./apps/*-web\" build",
    "lint": "pnpm -r --filter \"./apps/*-web\" lint",
    "typecheck": "pnpm -r --filter \"./apps/*-web\" typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "prettier": "^3.3.3"
  }
}
